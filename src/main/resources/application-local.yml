logging:
  level:
    ROOT: INFO
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
    com.sublime.whatsappclone: DEBUG
spring:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true